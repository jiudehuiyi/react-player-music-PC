import { connect } from 'react-redux';
import DataSeenLogin from "../../../components/dataSeen/dataSeenLogin"
const mapStateToProps = (state,ownProps)=>{
    return {}
}
const mapDispatchToProps = (dispatch,ownProps)=>{
    return {}
}
export default connect(mapStateToProps,mapDispatchToProps)(DataSeenLogin)